DATABASE_URL="postgresql://firstassist:firstassist@localhost:5432/firstassist"
PORT=4000
UPLOADS_DIR=./uploads
CORS_ORIGIN=http://localhost:5173

# Generate a real secret for anything beyond local dev, e.g.: openssl rand -hex 32
JWT_SECRET=dev-only-change-me

# Used by `npm run prisma:seed` to create the first admin login
ADMIN_EMAIL=admin@firstassist.local
ADMIN_PASSWORD=changeme123
